// @flow
import { Auth, Root } from './screens';

const routes = {
  HOME: {
    path: '/auth',
    component: Auth
  },
  ROOT: {
    path: '/root',
    component: Root
  }
};

export default routes;
