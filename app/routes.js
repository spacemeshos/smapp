// @flow
import { Auth, Main } from './screens';

const routes = {
  HOME: {
    path: '/auth',
    component: Auth
  },
  ROOT: {
    path: '/main',
    component: Main
  }
};

export default routes;
